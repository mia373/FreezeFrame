# Stage 1: Sync + Preprocessing
numpy
scipy
opencv-python
Pillow

# Stage 2: COLMAP
# COLMAP binary: apt install colmap (Linux cloud box) or download from github.com/colmap/colmap/releases (Windows)
# pycolmap wraps COLMAP C++ for sparse steps (extraction, matching, mapper) — no binary needed for these
pycolmap

# Stage 3: 4DGaussians
# 4DGS has its own requirements — install separately in the 4DGS repo clone
# This file covers the wrapper scripts only
torch
torchvision
plyfile

# Stage 4: Viewer
# Viewer is pure HTML/JS — no pip deps

# Server: Gemini Live proxy
websockets
google-genai
python-dotenv
requests

# Bullet-time pipeline
pydantic

# Shared
pyyaml
python-dotenv
requests
