{
  "framework": "vite",
  "buildCommand": "cd viewer && npm run build && cp -r ../commonthreads dist/precomputed",
  "outputDirectory": "viewer/dist",
  "installCommand": "cd viewer && npm install"
}
